# 怎么用 —— 全自动版（你的 Mac 已满足所有条件）

你的电脑已确认：claude ✅ codex ✅ python3 ✅
只差填一个 MiniMax key，就能一条命令跑完整个质检。

---

## 一次性设置（3 分钟）

### 第1步：进项目目录
```bash
cd ~/Desktop/qc-team
```

### 第2步：填 MiniMax key（重要）
```bash
cp .env.example .env
open -e .env
```
在打开的文件里，把 `MINIMAX_API_KEY=` 后面填上你**新生成的** key（别用之前贴出来那个，那个已作废）。
顺便核对 `MINIMAX_API_URL` 和 `MINIMAX_MODEL` 和你平台文档一致（不一致就改）。存盘关闭。

> 这个 .env 文件已被 .gitignore 挡住，**永远不会上传 GitHub**，key 安全。

### 第3步：装一个转换库（稿件是 Word 时用）
```bash
pip3 install python-docx --break-system-packages
```

---

## 每次质检一份稿件（一条命令）

### 稿件是 Word？先转纯文本：
```bash
python3 scripts/docx2txt.py ~/Desktop/学生作业.docx
```

### 然后跑：
```bash
python3 qc.py ~/Desktop/学生作业.txt
```

它会自动：
1. Crossref 真实核验所有 DOI
2. Claude 当主审核查文内匹配
3. Codex 当复核挑主审的错
4. MiniMax 综合出完整报告

报告自动存在 `reports/` 里。**全程你不用复制粘贴。**

---

## 先跑个 demo 验证能通

```bash
python3 qc.py samples/demo_稿件样例.txt
```
能出报告，就说明三个 AI 都接通了。

---

## 排错

- **claude/codex 报错"子命令不对"**：不同版本命令略有差异。
  - Claude 非交互：`claude -p "你的问题"`（脚本已用这个）
  - Codex 非交互：`codex exec "你的问题"`（脚本已用这个）
  - 若报错，在终端单独试 `claude -p "hi"` 和 `codex exec "hi"`，看哪个不通，告诉我具体报错。
- **MiniMax 报错**：多半是 .env 里的 URL 或模型名和你平台文档不一致，去文档核对。

---

## 上传 GitHub（给团队复用）

```bash
cd ~/Desktop/qc-team
git init
git add .
git commit -m "质检团队 v1 全自动版"
```
GitHub 建 **Private** 仓库，然后：
```bash
git remote add origin https://github.com/你的用户名/qc-team.git
git branch -M main
git push -u origin main
```

团队成员 clone 后，各自 `cp .env.example .env` 填自己的 key 即可。
**你的 key 不会被传上去，别人 clone 到的是空模板，各填各的。**
