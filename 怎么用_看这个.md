# QC-Team 中文操作说明

## 一次性配置

```bash
cd /你的路径/qc-team
cp .env.example .env
```

编辑 `.env`，填写自己的 MiniMax API Key、接口地址和模型名。这是一次性配置，以后不用重复填写。再安装 Word 读取依赖，并确认 Claude Code 和 Codex CLI 已安装、已登录：

```bash
python3 -m pip install -r requirements.txt
claude -p "只回复 OK"
codex exec "只回复 OK"
```

## 运行稿件

Word、TXT、Markdown 稿件都可以直接运行。最短命令：

```bash
./qc "/绝对路径/学生作业.docx"
```

程序会自动读取 Word 正文和表格，然后依次调用 Claude 主审、Codex 复核、MiniMax 整理。无需手动转换 TXT。也可以使用完整写法：

```bash
python3 qc.py "/绝对路径/学生作业.docx"
```

路径最方便的填写方法：先输入 `./qc `，再从 Finder 把 Word 文件直接拖进终端，最后按回车。旧版 `.doc` 请先另存为 `.docx`。

## 运行虚构样例

```bash
./qc samples/demo_稿件样例.txt
```

该命令会依次调用 Crossref、Claude、Codex 和 MiniMax，可能消耗 API 配额。结果保存在 `reports/`。

## 常见问题

- 找不到 `claude` 或 `codex`：先安装对应 CLI，并在终端完成登录。
- MiniMax 返回错误：核对 `.env` 中的 API URL、模型名和 Key。
- Word 转换缺少模块：运行 `python3 -m pip install python-docx`。
- 文件路径带空格或中文：使用绝对路径，并用英文双引号包住。
- 报告不是最终事实：无全文证据或模型之间存在分歧时，必须人工回到原始材料核验。

## 隐私提醒

不要把真实稿件、客户资料、报告或密钥提交到公开仓库。每次提交前检查：

```bash
git status
git diff --cached
```
