# QC-Team 中文操作说明

## 一次性配置

```bash
cd /你的路径/qc-team
cp .env.example .env
```

编辑 `.env`，填写自己的 MiniMax API Key、接口地址和模型名。确认 Claude Code 和 Codex CLI 已安装并登录：

```bash
claude -p "只回复 OK"
codex exec "只回复 OK"
```

## 运行稿件

TXT 稿件可直接运行：

```bash
python3 qc.py "/绝对路径/学生作业.txt"
```

DOCX 稿件需先转换：

```bash
python3 -m pip install python-docx
python3 scripts/docx2txt.py "/绝对路径/学生作业.docx"
python3 qc.py "/绝对路径/学生作业.txt"
```

如果出现 `PackageNotFoundError`，说明 DOCX 不存在或路径写错。先在 Finder 中确认文件位置，再把真实绝对路径放进引号。

## 运行虚构样例

```bash
python3 qc.py samples/demo_稿件样例.txt
```

该命令会依次调用 Crossref、Claude、Codex 和 MiniMax，可能消耗 API 配额。结果保存在 `reports/`。

## 常见问题

- 找不到 `claude` 或 `codex`：先安装对应 CLI，并在终端完成登录。
- MiniMax 返回错误：核对 `.env` 中的 API URL、模型名和 Key。
- Word 转换缺少模块：运行 `python3 -m pip install python-docx`。
- 文件路径带空格或中文：使用绝对路径，并用英文双引号包住。
- 报告不是最终事实：无全文证据或模型之间存在分歧时，必须人工回到原始材料核验。

## 隐私提醒

不要把真实稿件、客户资料、报告或密钥提交到公开仓库。每次提交前检查：

```bash
git status
git diff --cached
```
